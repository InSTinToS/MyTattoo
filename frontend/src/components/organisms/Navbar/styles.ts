import styled from 'styled-components'

export const Container = styled.nav`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;

  > ul {
    display: flex;
    align-items: center;

    width: 100%;
    height: 46px;
    padding: 0 24px;
  }
`
